services:
  quickheadlines:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3030:3030"
    environment:
      TZ: "America/Chicago"
    volumes:
       - ./feeds.yml:/app/feeds.yml:rw
    restart: unless-stopped

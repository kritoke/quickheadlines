1. Create a proposal describing the problem and goals (`proposal.md`) — ready
2. Add a design note for the insertion animation and loading indicator (`design.md`) — ready
3. Implement Elm code changes to load additional items on scroll and animate inserts (`ui/src/Pages/Timeline.elm`, `ui/src/Components/FeedItem.elm`) — blocked by design
4. Add persistence (optional) to store loaded items and scroll position in `sessionStorage` — pending
5. Add tests for loading behavior and animation timing — pending
6. Update changelog and docs — pending
